spring:

  datasource:
    url: jdbc:mysql://localhost:3306/newdb
    username: root
    password: focamonaca

  jpa:
    database: mysql
    show_sql: true
    hibernate:
      ddl-auto: create-drop

