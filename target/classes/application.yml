spring:

  datasource:
    url: jdbc:mysql://localhost:3306/newdb
    username: root
    password: PinoDaniele03

  jpa:
    database: mysql
    show_sql: true
    hibernate:
      ddl-auto: create-drop

  mvc:
    pathmatch:
      matching-strategy: ant_path_matcher
